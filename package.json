{
  "name": "fmed-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "verify:rev0": "node scripts/verify_rev0.mjs",
    "verify:regola0": "node scripts/verify_regola0.mjs",
    "predev": "npm run verify:rev0 && npm run verify:regola0",
    "dev": "vite",
    "prebuild": "npm run verify:rev0 && npm run verify:regola0",
    "build": "vite build --configLoader runner",
    "postbuild": "node scripts/verify_dist_rev0.mjs && node scripts/verify_dist_pointzero.mjs",
    "lint": "eslint .",
    "preview": "vite preview",
    "check": "npm run verify:rev0 && npm run verify:regola0 && npm run lint && npm run build && npm run verify:regola0",
    "gate:regola0": "npm run verify:rev0 && npm run verify:regola0 && npm run lint && npm run build && npm run verify:regola0",
    "release:check": "npm run gate:regola0 && node scripts/verify_dist_pointzero.mjs"
  },
  "dependencies": {
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "4.7.0",
    "eslint": "^10.3.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.6.0",
    "vite": "6.4.1"
  },
  "description": "FMED REV0 - ricostruzione grafica completa Regola 0"
}
