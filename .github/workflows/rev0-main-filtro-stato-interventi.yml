name: rev0-main-filtro-stato-interventi-disabled
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporaneo disattivato"
